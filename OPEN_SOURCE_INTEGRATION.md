# 开源股票生态融合决策

## 已融合

- AKShare：做成可选历史行情备用源和交叉验证源。默认安装包仍优先使用 Tushare、东方财富和腾讯；服务端需要时执行 `pip install -r requirements-research.txt`。
- Backtrader / vectorbt 的研究方法：没有复制其代码，而是在本项目内实现了小型可审计回测引擎。信号在收盘后确认、下一交易日开盘成交，并显式计入费用和滑点。
- OpenBB 的数据层思想：统一数据源状态、来源标签、显式降级和 `/api/system/data-sources` 状态接口。
- Qlib / VeighNa 的因子研究思想：保留为后续离线研究服务，不塞进桌面主进程。

## 暂不直接嵌入

- OpenBB：体积与功能面远超当前 A 股桌面助手，且数据提供商、扩展和授权需要逐项审查。吸收架构，不引入整套运行时。
- Backtrader：GPL-3.0，不直接链接进当前发行包；采用独立实现避免发行许可混杂。
- vectorbt / Qlib：更适合批量参数研究和模型实验，依赖重、内存占用大，后续应作为独立研究服务。
- vn.py / VeighNa：不接自动下单。当前产品继续保持“信号提醒 + 人工确认”，避免把研究工具误变成无风控交易终端。

## 回测红线

- 只允许真实历史行情；数据不可用时拒绝用模拟数据回测。
- 不允许未来函数；信号与成交至少错开一个交易日。
- 必须显示交易成本、滑点、最大回撤和基准收益。
- 明示停牌、涨跌停不可成交、退市样本和幸存者偏差尚未完整模拟。
